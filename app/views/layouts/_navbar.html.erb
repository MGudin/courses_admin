<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Mostra/Ocultar navegacion</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">
                CoursesAdmin
            </a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li>
                    <%= link_to 'Cursos', courses_path %>
                </li>
                <% if @course.try :persisted? %>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            Curso (<%= @course %>) <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li>
                                <%= link_to 'Estudiantes', course_students_path(@course) %>
                            </li>
                            <li>
                                <%= link_to 'Examenes', course_exams_path(@course) %>
                            </li>
                        </ul>
                    </li>
                <% end %>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <% if user_signed_in? %>
                    <li>
                        <%= link_to "#{current_user.email} - (Deslogear)", destroy_user_session_path, method: :delete, data: {confirm: 'seguro che'} %>
                    </li>
                <% else %>
                    <li>
                        <a href="<%= new_user_session_path %>">
                            Log In
                        </a>
                    </li>
                <% end %>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
