<h1>Notas del examen <%= @exam %> del <%= @course %></h1>

<table class="table table-condensed">
  <thead>
    
    <tr>
      <th class="">
        Estudiante
      </th>
      <th class="text-center">
        Nota
      </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @grades.each do |grade| %>
    
    <tr>
      <td> <%= grade.student %> </td>
      <td class="text-center"> <%= grade.grade %> </td>
      <td>
        <%= link_to 'ver',
            course_exam_grade_path(@course, grade.exam,grade),
            class: 'btn btn-success'%>
      </td>
      <td>
        <%= link_to 'Editar',
            edit_course_exam_grade_path(@course, grade.exam, grade),
            class: 'btn btn-primary' %>
      </td>
      <td><%= link_to 'Destroy',
              course_exam_grade_path(@course, grade.exam,grade),
              method: :delete,
              class: 'btn btn-danger',
              data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


